"""全局数值配置：所有可调的游戏数值集中于此，禁止散落硬编码。

数据驱动值（物品/职业/商店/作物/股票/地图）仍在 world_data/ 的种子 JSON 里；
代码里出现的任何可调数值（恢复速率、阈值、费用、延迟、距离、上限、概率）
都必须定义在本模块并由各服务 import —— 改动一处即全局生效。

数值基线对应 docs/world-rules.md 的规则编号（R6/R8/R9/R11/R12/R14/R15/
R18-R31、M4/M5/M8/M12、T6）。
"""

# --------------------------------------------------------------------------- #
# 需求节奏（每小时结算，见 WorldEngine._apply_hourly_needs）
# --------------------------------------------------------------------------- #
# 清醒状态下的基础每小时消耗/增长。
SATIETY_DRAIN_PER_HOUR = 1
ENERGY_DRAIN_PER_HOUR = 1
MOOD_DRAIN_PER_HOUR = 1
LONELINESS_GAIN_PER_HOUR = 1

# 各行动每小时恢复；睡眠快于等待（睡眠一晚应约等于一天的精力消耗）。
WAIT_ENERGY_PER_HOUR = 1
WAIT_MOOD_PER_HOUR = 2
SLEEP_ENERGY_PER_HOUR = 2
SLEEP_MOOD_PER_HOUR = 3

# 饱食度 = 0 时每小时额外精力消耗。
SATIETY_EMPTY_EXTRA_ENERGY_DRAIN = 1

# 需求条上限（0~NEEDS_MAX 钳制）。
NEEDS_MAX = 100

# 初始状态（character card 未覆盖时）。
INITIAL_SATIETY = 100
INITIAL_ENERGY = 100
INITIAL_MOOD = 100
INITIAL_LONELINESS = 0
INITIAL_MONEY = 3000

# 高优先级决策阈值 与 夜间睡觉引导。
MOOD_BOOST_THRESHOLD = 20
LONELINESS_BOOST_THRESHOLD = 80
NIGHT_START_HOUR = 22
NIGHT_END_HOUR = 7
NIGHT_SLEEP_ENERGY_THRESHOLD = 40

# --------------------------------------------------------------------------- #
# 睡眠与旅店
# --------------------------------------------------------------------------- #
HOTEL_LOCATION_ID = "village_hotel"
HOTEL_NIGHTLY_FEE = 85
SLEEP_MIN_MINUTES = 60
SLEEP_MAX_MINUTES = 480

# --------------------------------------------------------------------------- #
# R1 等待
# --------------------------------------------------------------------------- #
WAIT_MIN_MINUTES = 1
WAIT_MAX_MINUTES = 240
DEFAULT_WAIT_MINUTES = 60  # API 未给 minutes 时的默认等待时长

# R12：精力 = 0 强制休息时长（休息结束前禁止 move/work）。
FORCED_REST_MINUTES = 60
# R15：地点满员后等待空位的复查间隔。
CAPACITY_RECHECK_MINUTES = 30

# --------------------------------------------------------------------------- #
# R6 移动
# --------------------------------------------------------------------------- #
MINUTES_PER_STEP = 2
WEATHER_MULTIPLIERS = {"clear": 1.0, "cloudy": 1.0, "rain": 1.5, "snow": 2.0}

# --------------------------------------------------------------------------- #
# M12 日常开销与促销
# --------------------------------------------------------------------------- #
UPKEEP_PER_DAY = 120  # 每日 00:00 全额扣除；余额不足自动负债（余额可为负）
# 负债（唯一来源：每日生活开销超支，余额为负）的影响。
DEBT_MOOD_PENALTY_PER_DAY = 8  # 负债者每日 00:00 心情扣减（焦虑），下限 0
# 促销掷骰：确定性哈希 % PROMO_ROLL_DENOMINATOR < PROMO_ROLL_HITS ≈ 20%。
# 保持 % 10 < 2 的原始公式不变，避免改变既有确定性结果。
PROMO_ROLL_DENOMINATOR = 10
PROMO_ROLL_HITS = 2
PROMO_DISCOUNT_PERCENT = 20  # 促销日售价 = base_sell_price × (100 - 折扣)/100

# --------------------------------------------------------------------------- #
# M5/M8 决策节奏（游戏分钟）
# --------------------------------------------------------------------------- #
RETRY_DELAY = 10  # 工具被世界规则拒绝后
IDLE_DELAY = 30  # 决策后仍空闲（周期复评）
DEGRADE_DELAY = 20  # LLM 失败后
DEGRADE_WAIT_MINUTES = 15  # LLM 宕机期间的降级行动
MAX_CONSECUTIVE_FAILURES = 5
SKIP_DECIDE_DELAY = 15  # 观察缓存命中后
BUDGET_SKIP_DELAY = 30  # 世界 LLM 预算耗尽后的休眠节奏

# --------------------------------------------------------------------------- #
# R9/R21/M4 社交
# --------------------------------------------------------------------------- #
TALK_DISTANCE = 3  # 曼哈顿距离（含 0）
LONELINESS_RELIEF = 1  # 每条送达消息双方孤单的缓解量
PAIR_COOLDOWN_MINUTES = 60  # 同一对智能体对话冷却
MAX_TURNS = 6  # 单次对话最大消息轮数
MAX_MESSAGE_CHARS = 200
MAX_TALK_CHARS = 200
# E-full 对话锁：对话开始后双方被锁定（action_type="talk"），期间
# move/wait/sleep/work 排队到对话结束执行；TALK_LOCK_MINUTES 是硬上限，
# 到期（talk_expired）强制结束，防止无人说话时永久锁死。
TALK_LOCK_MINUTES = 15  # 单次对话锁定硬上限（游戏分钟）
TALK_REPLY_GRACE = 5  # 对话中未回复时的决策复评间隔（游戏分钟）

# --------------------------------------------------------------------------- #
# R22/R23 建造与种植
# --------------------------------------------------------------------------- #
MAX_BUILD_DISTANCE = 3  # 建造锚点距建造者的最大距离
MAX_PLANT_DISTANCE = 3  # 种植点距种植者的最大距离

# --------------------------------------------------------------------------- #
# R25/R26 班次考勤
# --------------------------------------------------------------------------- #
SHIFT_EARLY_WINDOW = 30  # 班次开始前可签到窗口（分钟）
SHIFT_LATE_LIMIT = 120  # 最晚签到 / 缺勤判定（分钟）
SHIFT_UPCOMING_REMIND = 60  # 班次开始前提醒（分钟）
ATTENDANCE_LATE_PENALTY = 2.0  # 迟到一次扣的出勤分
ATTENDANCE_ABSENT_PENALTY = 10.0  # 缺勤一次扣的出勤分

# --------------------------------------------------------------------------- #
# T6 记忆
# --------------------------------------------------------------------------- #
WORKING_MEMORY_CAP = 20  # 工作记忆只保留最新 N 条
REFLECTION_MINUTE_OF_DAY = 23 * 60 + 30  # 每日反思时刻 23:30
MAX_RETRIEVE_LIMIT = 20
WEIGHT_ENTITY = 0.35
WEIGHT_KEYWORD = 0.25
WEIGHT_IMPORTANCE = 0.20
WEIGHT_RECENCY = 0.15
WEIGHT_UNRESOLVED = 0.05

# --------------------------------------------------------------------------- #
# T6-5 关系
# --------------------------------------------------------------------------- #
INTENT_DELTAS: dict[str, dict[str, int]] = {
    "greet": {"familiarity": 2, "affection": 1},
    "chat": {"familiarity": 2, "affection": 1},
    "offer": {"familiarity": 2, "affection": 1},
    "ask_help": {"familiarity": 2, "affection": 2},
    "leave": {"familiarity": 1, "affection": 0},
}
DEFAULT_INTENT_DELTAS = {"familiarity": 1, "affection": 0}
CLAMP_MAIN = (0, 100)  # familiarity/trust/affection/resentment 钳制
CLAMP_DEBT = (0, 1000)  # debt 钳制

# --------------------------------------------------------------------------- #
# R18 股票
# --------------------------------------------------------------------------- #
STOCK_NOISE_RANGE = 2  # 每小时确定性噪声 ±2
DIV_BUSINESS_PER_SHARE = 3  # 分红 = max(1, 当日经营数 // 3)（M19 由 5 下调增强）
MAX_SHARES = 9999  # 单笔交易上限（schema le 镜像）

# --------------------------------------------------------------------------- #
# M17 经理利润分成
# --------------------------------------------------------------------------- #
# 每日 00:00 从公司金库按当日净利润的该百分比给经理分成；
# 公司当日亏损或金库不足以支付时不发（历史流水聚合，不含初始资金）。
MANAGER_PROFIT_SHARE_PERCENT = 20

# --------------------------------------------------------------------------- #
# M18 创业与个人商店
# --------------------------------------------------------------------------- #
OPEN_SHOP_CAPITAL = 100  # 开店资本门槛（R39.5：只校验不扣款；M19 由 150 下调）
STALL_STOCK_CAP = 20  # 个人店每商品货架容量上限
STALL_INITIAL_STOCK = 5  # 开店首单上架件数（min(持有量, 该值)）
STALL_MAX_PRODUCTS = 3  # 单店最多商品种类
PRICE_MAX_MULT = 2.0  # 个人店售价上限倍数（1 ~ base_price × 该值）
STALL_BUY_MAX_MULT = 1.0  # M19：个人店收购价上限倍数（0 ~ base_price × 该值）
STALL_OPEN_HOUR = 6  # 摊位/荒地店开门小时（R8 复用）
STALL_CLOSE_HOUR = 22  # 摊位/荒地店关门小时
STALL_CAPACITY = 4  # 摊位/荒地店同时容纳最大人数
STALL_MAX_DISTANCE = 3  # 荒地店目标格距发起者最大曼哈顿距离
STORE_STOCK_INITIAL_PRICE = 10  # 个人店上市行初始股价（R18.2 计数）

# --------------------------------------------------------------------------- #
# R19 转账 / 赠物
# --------------------------------------------------------------------------- #
MAX_TRANSFER_AMOUNT = 1_000_000  # 防御性上限（真实上限是余额）
MAX_GIFT_QUANTITY = 99  # 防御性上限（真实上限是持有量）

# --------------------------------------------------------------------------- #
# 引擎与神谕
# --------------------------------------------------------------------------- #
TICK_INTERVAL = 0.1  # 引擎真实秒/帧
VALID_SPEEDS = (1, 2, 5, 10)
VALID_WEATHERS = {"clear", "cloudy", "rain", "snow"}

# --------------------------------------------------------------------------- #
# 观察文本（LLM 提示词裁剪）
# --------------------------------------------------------------------------- #
OBSERVATION_MAX_CHARS = 50000
OBSERVATION_MAX_UNREAD_MESSAGES = 3
OBSERVATION_MAX_SHOP_PRODUCTS = 6

# --------------------------------------------------------------------------- #
# 演示/测试专用（fake provider）
# --------------------------------------------------------------------------- #
FAKE_SATIETY_THRESHOLD = 20  # fake LLM 判定"饿了"去吃饭的饱食度阈值

# --------------------------------------------------------------------------- #
# OpenAI Agents SDK 运行循环（provider 内部，非游戏规则）
# --------------------------------------------------------------------------- #
OPENAI_SDK_MAX_TURNS = 4  # 单次决策的 SDK 工具调用循环上限
